// var regex = /^[a-zA-Z]*$/;
function rot13(message) {
  var alphabet = 'abcdefghijklmnopqrstuvwxyz'
  var solution = ''

  function contains(letter) {
    if  
  }

  function rotate(letter) {
    var index = a.indexOf(letter)    

  }

  for (var i = 0; i < message.length; i++) {
    if (contains(letter)) {
      
    }
    if (message[i] === message[i].toUpperCase()) {
       
    } 
}
